services:
  server:
    build:
      context: ./server
    environment:
      PORT: 8080
    ports:
      - "8080:8080"

  client:
    build:
      context: ./client
    environment:
      VITE_PROXY_TARGET: http://server:8080
    ports:
      - "5173:5173"
    depends_on:
      - server